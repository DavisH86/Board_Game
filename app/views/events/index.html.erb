<p class="small-6 center">
  If you're looking for a time to relax and have fun after a long day,
  use this app to bring together innovation crowd for an evening of board games and great times.
  Don't know how to play? New players are welcome to meet and learn new
  games for the first time.
</p>

<div>
  <%= form_for(:event, {url: search_events_path, method: :get}) do |f|%>
    <p class="small-9 medium-9 Large-9 columns"><%= f.text_field :name, placeholder: "Search Events.." %></p>
    <p class="small-3 medium-3 Large-3 columns"><%= f.submit 'Search' %></p>
  <% end %>
</div>

<h1 class="Title">Event Index</h1>
<div class="indexLinks">
  <%= link_to "Location", locations_path %> |
<%= link_to "Add New Event", new_event_path %> |
<%= link_to "Group Page", groups_path %> |
<%= link_to "Board Games", boardgames_path %>
</div>
<table class="indextable">
  <thead>
    <tr>
      <th>Title</th>
      <th>Posted</th>
      <th>Date</th>
      <th>Organizer</th>
      <th colspan="3"></th>
    </tr>

  </thead>

  <tbody>
    <% @events.each do |event| %>
    <tr>
      <td>
        <%= link_to event.name, event_path(event) %><br>
      </td>
      <td>
        <%= "Posted on: #{event.created_at.strftime("%B %d %Y")}" %>
      </td>
      <td>
        <% if event.eventdate != nil %>
          <%= "Date: #{event.eventdate.strftime("%m/%d/%Y at %I:%M%p")}"%>
        <% end %>
      </td>
      <td>
        <%= "Organized by: #{event.organizer.first_name} #{event.organizer.last_name}" %>
      </id>
    </tr>
    <% end %>


  </tbody>
</table>
